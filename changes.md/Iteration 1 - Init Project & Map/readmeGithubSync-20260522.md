# 2026-05-22 — Root README + change-log visibility + GitHub push

## Summary

Mirrored **change log / agent handoff** guidance into the **root `README.md`** (GitHub’s default landing document) while keeping **`changes.md/README.md`** as the folder README for the `changes.md/` directory. Updated **`docs/iteration-01-summary.md`** links to match the **Iteration 1** subfolder layout. Pushed **`main`** to **`origin`**.

## Notes

- GitHub only auto-renders **`README.md` at the repository root** on the repo home page; subfolder READMEs render when you open that folder. Duplicating the short conventions block on the root README makes the same information visible on first load.

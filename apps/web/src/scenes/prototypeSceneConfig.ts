/**
 * Dev playground layout constants (floor only).
 * Interactable definitions live in `features/interaction/interactableRegistry.ts`.
 */
export const PROTOTYPE_SCENE_CONFIG = {
  floor: {
    size: 12,
    halfExtent: 6,
  },
} as const

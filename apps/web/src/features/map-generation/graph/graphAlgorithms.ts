/**
 * Reserved for weighted pathing (e.g. A* on room centers), spanning-tree dungeon
 * backbones (Kruskal / Prim), lock/key DAGs, and other graph algorithms that are
 * not needed for the current unweighted room graph.
 *
 * Unweighted traversal lives in {@link ./graphTraversal}.
 */

export {}
